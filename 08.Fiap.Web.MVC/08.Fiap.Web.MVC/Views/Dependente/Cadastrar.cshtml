@model _08.Fiap.Web.MVC.Models.Dependente

@{
    ViewBag.Title = "Cadastrar";
}

<h2>Cadastro de Dependente</h2>

@if (TempData["msg"] != null)
{
    <div class="alert alert-success">
        @TempData["msg"]
    </div>
}

@using (Html.BeginForm())
{
    <div class=" = form-group">
        @Html.LabelFor(d => d.Nome)
        @Html.TextBoxFor(d => d.Nome, new { @class = "form-control" })
    </div>
    <div class=" = form-group">
        @Html.LabelFor(d => d.DataNascimento)
        @Html.TextBoxFor(d => d.DataNascimento, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(r => r.Responsavel)
        @Html.DropDownListFor(r => r.ResponsavelId, ViewBag.responsaveis as SelectList, "Selecione", new { @class = "form-control"})
    </div>
    <input type="submit" value="Cadastrar" class="btn btn-primary" />
}

